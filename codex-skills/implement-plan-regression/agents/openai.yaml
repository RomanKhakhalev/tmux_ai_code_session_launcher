interface:
  display_name: "Implement Plan Regression"
  short_description: "Review diffs for UX and workflow ownership drift"
  default_prompt: "Use $implement-plan-regression to check staged and unstaged diffs against this plan file and the thread history for visible regressions, workflow drift, and parallel Tellis W/G/M or accounting ownership."
